# Copy .env.example to .env file and fill in your values below.
ANTHROPIC_API_KEY="Your-api-key"
# SEC requires a contact email in the User-Agent header to identify requesters.
SEC_USER_EMAIL="your_email@example.com"
